package com.shuffling.algo;

public enum RemovalCount {
	/**
	 * Their Ordinal points to how much Weight reduction each signifies
	 * Make sure that the Ordinal is not > 5
	 * As of now: MEDIUM, HIGH & VERY_HIGH are in use
	 */
	NONE, VERY_LOW, LOW, MEDIUM, HIGH, VERY_HIGH;
}